include:
  - UserFixture.yaml
  - TagFixture.yaml

App\Entity\Article:
  article{1..50}:
   titre (unique): <word(2, true)>
   content: <generateContent()>
   user: "@userAdmin"
   categorie: <randomElements(@categorie{1..10})>
   createdAt: <generateDate()>