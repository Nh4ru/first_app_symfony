{{ form_start(form, { attr: { class: 'form-filter p-3 card' } }) }}
{{ form_row(form.query) }}
<h3 class="mb-3">Catégories :</h3>
{{ form_row(form.categories, { attr: { class: 'd-flex flex-wrap gap-2' } }) }}
<h3 class="mb-3">Auteur :</h3>
{{ form_row(form.author, { attr: { class: 'd-flex flex-wrap gap-2' } }) }}
<button type="submit" class="btn btn-primary">Filtrer</button>
{{ form_end(form) }}
