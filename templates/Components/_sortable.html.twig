{{ knp_pagination_sortable(articles, 'Date ', 'a.createdAt', { direction: 'DESC', class: 'btn btn-primary' }) }}
{{ knp_pagination_sortable(articles, 'Titre ', 'a.titre', { direction: 'ASC', class: 'btn btn-primary' }) }}
