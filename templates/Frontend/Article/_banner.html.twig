{% for image in article.images|slice(0, 1) %}
  {% set imgSrc = "style='background-image: url(" ~ vich_uploader_asset(image, 'imageFile') ~ ");'" %}
  {% include 'Components/_headerBanner.html.twig' with { styleImage: imgSrc, headerBanner: article.titre } %}
{% endfor %}
