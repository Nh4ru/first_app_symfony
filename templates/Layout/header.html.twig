<header>
	<nav class="navbar navbar-expand-lg navbar-light bg-primary">
		<div class="container-fluid">
			<a class="navbar-brand text-light" href={{ path('home') }}>My App Symfony</a>
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="collapse navbar-collapse" id="navbarNav">
				<ul class="navbar-nav">
					<li class="nav-item">
						<a class="nav-link active text-light" aria-current="page" href="#">Home</a>
					</li>
					<li class="nav-item">
						<a class="nav-link text-light" href="#">Features</a>
					</li>
					<li class="nav-item">
						<a class="nav-link text-light" href="#">Pricing</a>
					</li>
					<li class="nav-item">
						<a class="nav-link text-light disabled">Disabled</a>
					</li>
				</ul>
			</div>
			{% if app.user %}
				{% if is_granted('ROLE_ADMIN') %}
					<div class="ml-left">
						<a href="{{ path('admin') }}" class="btn btn-secondary">Admin</a>
					</div>
				{% endif %}
				<div class="ml-left ml-4">
					<a href="{{ path('logout') }}" class="btn btn-danger">Se deconnecter</a>
				</div>
			{% else %}
				<div class="ml-left">
					<a href="{{ path('login') }}" class="btn btn-secondary">Login</a>
				</div>
			{% endif %}
		</div>
	</nav>
</header
